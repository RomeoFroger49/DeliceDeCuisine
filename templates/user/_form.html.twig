

{{ form_start(form) }}
    <div class="flex flex-col items-center">
        <div class="w-2/3">
            {{ form_row(form.email, {  'name': 'Email :'}) }}
            {{ form_row(form.username, {'name': 'Identifiant :'}) }}
        </div>

        <button class="w-1/4 border border-black rounded-2xl hover:border-red-500 hover:text-red-500 ">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
